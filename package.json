{
  "name": "shipstation-printful-bridge",
  "version": "3.13.1",
  "description": "ShipStation to Printful bridge with automatic OLD-SKU product matching and automatic Printful catalog refresh on cache miss.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
